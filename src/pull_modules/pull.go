package pull_modules

// A variable which should be accessed from the func main().
//
// Accessing this variable from main() will automatically pull-in
// the optional modules into the final executable.
var Pull byte
